## Agent skills

### Issue tracker

이슈는 GitHub Issues에서 관리한다(`gh` CLI). 상세는 `docs/agents/issue-tracker.md` 참조.

### Domain docs

single-context: 루트 `CONTEXT.md` + `docs/adr/`. 상세는 `docs/agents/domain.md` 참조.

### 작업 실행 (rubber-workflow)

브레인스토밍으로 확정한 작업이나 `docs/product/roadmap.md`의 단계(R0~R9) 하나를 실행할 때는
rubber-workflow(DELTA 단위, 탄력적 추가, `dev` 브랜치 + 재그룹화 병합)를 따른다. 상세는
`docs/agents/rubber-workflow.md` 참조.
